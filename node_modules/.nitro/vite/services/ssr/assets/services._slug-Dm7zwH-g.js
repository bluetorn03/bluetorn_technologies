import { n as servicesBySlug } from "./services-data-Ca5PnO-D.js";
import { createFileRoute, lazyRouteComponent, notFound } from "@tanstack/react-router";
//#region src/routes/services.$slug.tsx
var $$splitNotFoundComponentImporter = () => import("./services._slug-BTl6UoK6.js");
var $$splitComponentImporter = () => import("./services._slug-8Zi2j5WZ.js");
var Route = createFileRoute("/services/$slug")({
	loader: ({ params }) => {
		const service = servicesBySlug[params.slug];
		if (!service) throw notFound();
		return service;
	},
	head: ({ loaderData }) => {
		if (!loaderData) return { meta: [{ title: "Service not found | BLUETORN" }, {
			name: "robots",
			content: "noindex"
		}] };
		const s = loaderData;
		return {
			meta: [
				{ title: `${s.title} | BLUETORN Technologies` },
				{
					name: "description",
					content: s.hero
				},
				{
					property: "og:title",
					content: `${s.title} | BLUETORN`
				},
				{
					property: "og:description",
					content: s.hero
				},
				{
					property: "og:type",
					content: "website"
				},
				{
					name: "twitter:card",
					content: "summary_large_image"
				}
			],
			links: [{
				rel: "canonical",
				href: `/services/${s.slug}`
			}]
		};
	},
	component: lazyRouteComponent($$splitComponentImporter, "component"),
	notFoundComponent: lazyRouteComponent($$splitNotFoundComponentImporter, "notFoundComponent")
});
//#endregion
export { Route as t };
