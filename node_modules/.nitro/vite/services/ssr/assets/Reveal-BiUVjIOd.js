import { jsx, jsxs } from "react/jsx-runtime";
import { motion } from "motion/react";
//#region src/components/site/SectionEyebrow.tsx
function SectionEyebrow({ children, tone = "dark" }) {
	return /* @__PURE__ */ jsxs("div", {
		className: `inline-flex items-center gap-2 text-[0.7rem] font-semibold uppercase tracking-[0.28em] ${tone === "light" ? "text-teal" : "text-teal"}`,
		children: [/* @__PURE__ */ jsx("span", { className: "h-px w-8 bg-teal/60" }), children]
	});
}
//#endregion
//#region src/components/site/Reveal.tsx
var variants = {
	hidden: {
		opacity: 0,
		y: 24
	},
	show: (i = 0) => ({
		opacity: 1,
		y: 0,
		transition: {
			duration: .7,
			delay: i * .06,
			ease: [
				.2,
				.7,
				.2,
				1
			]
		}
	})
};
function Reveal({ children, i = 0, className = "" }) {
	return /* @__PURE__ */ jsx(motion.div, {
		className,
		variants,
		initial: "hidden",
		whileInView: "show",
		viewport: {
			once: true,
			margin: "-80px"
		},
		custom: i,
		children
	});
}
//#endregion
export { SectionEyebrow as n, Reveal as t };
